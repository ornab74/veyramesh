name: veyra_observatory
description: Flutter research console for VeyraMesh deterministic simulation studies.
publish_to: none
version: 3.0.0+1
resolution: workspace

environment:
  sdk: ">=3.8.0 <4.0.0"

flutter:
  uses-material-design: true

dependencies:
  flutter:
    sdk: flutter
  veyra_lab: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
